{
	"$schema": "http://json-schema/draft-04/schema#",
	"description": "a code list for POST",
	"type": "array",
	"items": {
		"type": "object",
		"properties": {
			"id": {
				"type": "string",
				"description": "the id of the code"
			},
			"status": {
				"enum": [ "active" ],
				"description": "the status of the code; 0: active, 1: redeemed, 2: disabled, 3: assigned",
				"default": "active"
			}
		},
		"additionalProperties": {
			"type": [ "string", "object", "boolean", "array", "number" ],
			"description": "a code property added by the admin, a key:value pair of any data type"
			},
		"patternProperties": {
			"[$.]|^voucherId$|^ownerId$": {
				"description": "forbidden characters in properties - dollar signs $ and dots .; forbidden names in property keys - voucherId",
				"not": {}
			}
		},
		"required": [ ]
	}
}